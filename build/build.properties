# Base Directory
base.dir=.

# Define a directory to hold build results
base.results.dir=${base.dir}/results

# Define a directory to hold distribution files
dist.dir=${base.dir}/dist

# Define paths that we explicitly want to include/exclude from distribution
# We'll reference these explicitly in our build script
dist.build.dir=build/
dist.tests.dir=tests/
